post:
  operationId: createExample
  summary: Create a new example
  requestBody:
    required: true
    content:
      application/json:
        schema:
          oneOf:
            - $ref: "#/components/schemas/new_schema"
            - $ref: "#/components/schemas/another_schema"
  responses:
    "200":
      description: OK
    "400":
      description: KO
